apiVersion: v1
kind: PersistentVolume

metadata:
  name: magento-media-pv

spec:

  storageClassName: standard

  capacity:
    storage: 20Gi

  accessModes:
    - ReadWriteMany

  persistentVolumeReclaimPolicy: Retain

  nfs:
    server: nfs-server
    path: /